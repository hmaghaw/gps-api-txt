services:
  api-txt:
    build: .
    container_name: gps-api-txt
    ports:
      - "9000:9000"
    volumes:
      - ./app:/app/app  # live code mount for local dev; remove for production
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped